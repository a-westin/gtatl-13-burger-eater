<div class="card">
    <div class="card-header text-center">
        <h4>Hope they were tasty!</h4>
    </div>
    <div class="card-body">

        {{#each eatenList}}
            <div>{{id}}. {{burger_name}}</div>
        {{/each }}

    </div>
</div>